# C4 model — agent-lsp

Live C4 model is maintained via the **architect-c4** MCP
(`architecture.runmcp.ru`), workspace `ws-agent-lsp-c4`.

Related: [OVERVIEW.md](OVERVIEW.md) · [ADL](../adr/README.md)

## Viewer links

| View | URL |
|------|-----|
| All layers | https://architecture.runmcp.ru/view/ws-agent-lsp-c4 |
| Context (L1) | https://architecture.runmcp.ru/view/ws-agent-lsp-c4?layer=context |
| Containers (L2) | https://architecture.runmcp.ru/view/ws-agent-lsp-c4?layer=container&parent=sys-agent-lsp |
| Components — MCP Server | https://architecture.runmcp.ru/view/ws-agent-lsp-c4?layer=component&parent=ctr-mcp-server |
| Flow: scout onboard | https://architecture.runmcp.ru/view/ws-agent-lsp-c4/flows/flow-scout-onboard |
| ADRs | https://architecture.runmcp.ru/view/ws-agent-lsp-c4/adrs |

## Model summary

**Context:** AI Agent / Developer → Cursor → agent-lsp (Scout LSP); agent-lsp →
Docker Engine, Git remotes / mirrors.

**Containers** (inside agent-lsp): Caddy, MCP Server, Runtime Health Worker,
SQLite State, Git Worktree Store, LSP Runtime Containers.

**Components** (inside MCP Server): Scout Tools API, RuntimeHub,
LspClient / TcpTransport, TaskStore + ScoutWorker, GitService, DockerService,
StateStore.

**ADRs in model:** adr-0007 (session-held containers), adr-0012 (runtime-health
worker) — status `proposed` in architect-c4 (process token required for
`accepted`).

## How to edit

Use architect-c4 MCP tools (session + explicit `workspace_id` on checkout):

1. `create_session` → `create_project` → `checkout_workspace` (**must** pass
   non-null `workspace_id`)
2. `upsert_element` / `upsert_relationship` / `upsert_flow` / `upsert_adr`
3. `validate_model` → `get_view_links` with
   `base_url=https://architecture.runmcp.ru`

V1 atom canon allows relationship edges only between
`person` / `software_system` / `code` / `external`. Containers and components
nest via `parent_id`; use flows for dynamic container-level interaction.
